import Product from './Product';

type transaction = {
    title: string,
    displayName: string,
    price: number,
    message: string,
    transactionId: string,
    userId: string
}

export default transaction;